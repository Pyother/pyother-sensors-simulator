import { createTheme } from '@mui/material/styles';

const colorsTheme = createTheme({
    palette: {
        primary: {
            main: '#6e73d7',
        },
        secondary: {
            main: '#c232b5',
        }
    }
});

export default colorsTheme;